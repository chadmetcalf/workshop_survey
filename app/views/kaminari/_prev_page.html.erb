<li class=<%= "disabled" if current_page.first?%>>
  <%= link_to raw(t 'views.pagination.previous'), (current_page.first? ? "" : url),
              rel: 'next', remote: remote, class: ("disabled" if current_page.first?) %>
</li>
