<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="ROBOTS" content="NOODP" />
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
  <title><%= title %></title>
  <%- if Rails.env.production? %>
    <%= render 'rollbar' %>
  <%- end %>
  <%= stylesheet_link_tag :application, media: "all" %>

  <!-- Latest compiled and minified CSS -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/normalize/6.0.0/normalize.min.css" integrity="sha256-VbcxqgMGQYm3q8qZMd63uETHXXZkqs7ME1bEvAY1xK8=" crossorigin="anonymous" />
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
  <%= render "css_overrides" %>

  <%= csrf_meta_tags %>
</head>
<body class="<%= body_class %>">
  <div class="container">
    <div class="header clearfix">
      <nav class="blog-nav">
        <a class="navbar-brand" href="/">
          <%= image_tag('/assets/MSTS.png')%>
        </a>
        <ul class="nav nav-pills pull-right">
          <% if signed_in? %>
            <li><%= button_to 'Sign out', sign_out_path, method: :delete, class: "btn btn-default navbar-btn"%></li>
          <% else %>
            <li><%= link_to 'Sign in', sign_in_path %></li>
          <% end %>
      </nav>
    </div>
    <%= flashes %>
    <%= yield %>
  </div>

  <script src="https://unpkg.com/vue"></script>
  <%= render "javascript" %>
</body>
</html>
